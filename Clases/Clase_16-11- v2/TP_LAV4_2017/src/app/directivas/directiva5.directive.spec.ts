import { Directiva5Directive } from './directiva5.directive';

describe('Directiva5Directive', () => {
  it('should create an instance', () => {
    const directive = new Directiva5Directive();
    expect(directive).toBeTruthy();
  });
});
