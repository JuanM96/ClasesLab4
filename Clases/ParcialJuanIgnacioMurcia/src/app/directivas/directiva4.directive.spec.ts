import { Directiva4Directive } from './directiva4.directive';

describe('Directiva4Directive', () => {
  it('should create an instance', () => {
    const directive = new Directiva4Directive();
    expect(directive).toBeTruthy();
  });
});
